# MygoCode 项目了解记录

> 记录日期：2026-07-26
>
> 用途：作为后续项目问答的事实依据。新发现的项目事实应更新本文档；具体问答过程写入《问答记录.md》。

## 1. 项目定位

MygoCode 是一个使用 Go 编写的终端 AI 编程助手，提供交互式 TUI、浏览器远程 UI、文件和命令工具、多个 LLM 提供商、MCP 扩展以及多智能体协作能力。

模块路径为 `mygocode`，Go 版本要求为 `1.25.0`。

## 2. 启动入口

入口文件：`cmd/mygocode/main.go`。

启动顺序大致如下：

1. 识别 `--teammate`，进入团队成员工作进程。
2. 识别项目授权参数：`--trust-project`、`--deny-project`、`--revoke-project-trust`。
3. 识别 `--remote [address]`，进入浏览器远程 UI 模式。
4. 加载配置和 Hook。
5. 默认启动 Bubble Tea TUI。

主要命令：

```text
go build -o mygocode ./cmd/mygocode
./mygocode
./mygocode --remote
./mygocode --teammate --team-name <team> --agent-name <member>
```

## 3. 主要目录

| 目录 | 职责 |
| --- | --- |
| `cmd/mygocode` | CLI 入口、远程模式入口、teammate 进程入口 |
| `internal/agent` | 主 Agent 循环、事件流、技能绑定、Agent profile |
| `internal/agents` | 子 Agent 定义、加载、TaskManager、工具过滤 |
| `internal/llm` | Anthropic、OpenAI、OpenAI-compatible 客户端和模型解析 |
| `internal/tools` | 内置工具、工具接口、注册表、延迟工具发现 |
| `internal/config` | YAML 配置、提供商校验、MCP 配置、项目信任状态 |
| `internal/permissions` | 权限模式、规则引擎、路径沙箱、危险命令检测和人工确认 |
| `internal/mcp` | MCP stdio、Streamable HTTP、SSE 连接及工具包装 |
| `internal/tui` | Bubble Tea 终端界面和斜杠命令分发 |
| `internal/remote` | HTTP 页面和 WebSocket 远程 UI |
| `internal/conversation` | 内存中的对话、工具调用和工具结果结构 |
| `internal/session` | JSONL 会话保存、恢复、fork、压缩边界和 HTML 导出 |
| `internal/compact` | 上下文 token 估算、自动/手动压缩和恢复状态 |
| `internal/memory` | 用户级和项目级自动记忆 |
| `internal/skills` | `SKILL.md` 技能加载和执行 |
| `internal/hooks` | 生命周期、命令、HTTP、prompt、agent Hook |
| `internal/teams` | TeamManager、团队成员、消息和 teammate 协作 |
| `internal/todo` | 持久化任务列表和任务工具 |
| `internal/worktree` | Git worktree 隔离、恢复和清理 |
| `internal/sandbox` | 可选的 OS 级命令沙箱 |

## 4. 配置与信任

全局配置默认位于：

```text
~/.mygocode/config.yaml
```

项目配置位于：

```text
<project>/.mygocode/config.yaml
<project>/.mygocode/config.local.yaml
```

项目配置默认不加载。授权记录位于：

```text
~/.mygocode/trusted-projects.json
```

只有项目状态为 `trusted` 时，项目级配置才会与全局配置合并。`denied` 和未决定状态都不会加载项目配置。没有可用全局配置且项目未授权时，会出现：

```text
No usable config found. Configure ~/.mygocode/config.yaml or trust this project in the TUI.
```

支持的聊天协议：`anthropic`、`openai`、`openai-compat`。API key 优先使用配置字段，否则读取对应环境变量。

## 5. Agent 工作流

TUI 初始化单个 provider 时会：

1. 加载技能和自定义指令。
2. 创建 LLM client、会话 ID、文件历史和默认工具注册表。
3. 创建权限 Checker，配置项目路径、记忆目录和受保护路径。
4. 注册 Todo、Agent、Team、Worktree、Plan、ToolSearch 等扩展工具。
5. 异步连接 MCP 并注册 MCP 工具。
6. 创建 `agent.Agent` 并开始接收用户消息。

`agent.Agent.Run` 是事件驱动的循环：

```text
读取会话上下文
  → 注入 instruction/memory/system reminder
  → 生成当前可见工具 schema
  → 调用 LLM 流式接口
  → 输出文本或处理 tool_use
  → 通过 Checker 判断 allow/deny/ask
  → 执行工具并写入 tool_result
  → 处理通知和上下文压缩
  → 继续下一轮，直到完成、取消、出错或达到最大迭代数
```

工具 schema 支持延迟发现。未发现的延迟工具不会直接发送给模型，模型可以通过 `ToolSearch` 搜索并标记发现。

## 6. 内置工具与权限

默认工具包括 `ReadFile`、`WriteFile`、`EditFile`、`Bash`、`Glob`、`Grep`。扩展工具还包括任务、子 Agent、团队、技能、MCP、worktree 和媒体相关工具。

权限检查的大致优先级：

1. Plan mode 对计划文件的写入例外。
2. 安全只读命令自动允许。
3. 启用 OS 沙箱后的命令检查。
4. 危险命令检测。
5. 文件路径沙箱和受保护路径检查。
6. 用户/项目规则文件。
7. 当前会话的 allow-always 记录。
8. 权限模式决定 allow、deny 或 ask。
9. ask 交给 TUI 或远程 UI 进行人工确认。

默认路径沙箱允许项目根目录和临时目录，同时保护项目 `.mygocode/config.yaml`、权限文件和 skills 目录不被 Agent 写入。

## 7. 会话、fork 与导出

会话保存于项目的 `.mygocode/sessions/<session-id>.jsonl`。内存对话使用 `internal/conversation.Manager`，磁盘记录用于恢复。

`ForkSession` 会复制源 JSONL，写入新的 session metadata，并记录 `ParentSessionID`。新会话是独立的，后续消息不会自动回写源会话；项目当前没有父子树导航或合并机制。

`ExportHTML` 生成项目内的自包含 HTML，执行 HTML 转义、API key/Bearer/password/secret 脱敏，并限制过长的工具输出。导出不会修改原始 JSONL。

上下文压缩分为工具结果预算处理和对话摘要压缩。压缩边界会写入 session，恢复时可用摘要加最近消息尾部重建上下文。

## 8. TUI 与斜杠命令

TUI 基于 Bubble Tea。主要命令包括：

```text
/help /status /clear /compact /plan /review
/resume /session /fork /export /trust
/memory /skills /permission /sandbox /mcp
/agent /tasks
```

OpenCode 改进中的 `build`、`plan`、`review` 是主 Agent profile；`/tasks` 只读展示子任务和团队成员汇总。P2 的 `mygocode run` 非交互入口已取消，没有实现。

## 9. MCP

项目配置支持 stdio、Streamable HTTP 和 SSE。MCP 工具会包装为：

```text
mcp__<sanitized-server-name>__<sanitized-tool-name>
```

MCP 连接失败会记录错误并继续启动，不会让全部 TUI 直接失败。

当前 Codex 全局还注册了 DeepWiki MCP：

```text
https://mcp.deepwiki.com/mcp
```

## 10. 测试与当前状态

测试文件分布在各个 `internal/*` 包和 `cmd/mygocode`。当前已确认：

- `go list ./...` 正常。
- `go test ./cmd/mygocode` 通过。
- `go test ./...` 在约 120 秒后超时；已经看到 `cmd/mygocode` 包通过，剩余长时间测试未完成。

当前工作区已有用户自己的修改：`report.md`；另有未跟踪文件 `简历.tex`。后续修改时应避免覆盖它们。

## 11. 后续问答规则

- 回答优先依据代码和测试，不凭 README 的宣传性描述猜测。
- 涉及行为时，优先指出入口文件、核心类型和调用链。
- 不确定的内部实现会明确说明“代码中未发现”或“尚未验证”。
- 每次问答的用户问题、回答摘要、依据文件和待确认点，追加到《问答记录.md》。
