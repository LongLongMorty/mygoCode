# 测试部分：项目测评现状与业内主流手段

> 整理自会话问答（2026-08-05）："目前我的项目中进行了哪些测评 agent 工作，目前业内主流测评手段有哪些呢"

## 一、项目内已有的测评工作

### 1. 唯一的真 Benchmark：MCP 懒加载（`internal/tools/deferred_benchmark_test.go`）

项目里唯一带量化指标的测评：

- **场景设计**：58 个模拟真实 MCP 工具（按 Grafana/Playwright 的实际 schema 风格构造，每个 800–1200 bytes）+ 10 轮模拟对话
- **实测结果**：全量加载 158,685 estimated tokens → 延迟加载 25,102，**节省 84.2%**
- **断言**：阈值 `>= 80%`，跑不过就 fail
- `report.md` 有完整复盘：84.2% vs 简历写的 85%——结论是"约 84%"更准确

### 2. 真实 LLM 端到端验证（`internal/agent/agent_live_test.go`）

3 个 live 测试（需 API key，无 key 自动 skip，已在本机跑通）：

| 测试 | 验证内容 | 耗时 |
| --- | --- | --- |
| `TestLiveSimpleChat` | 基础对话链路 | 5.4s |
| `TestLiveFrontendDesignSkill` | 多轮对话 + 技能调用 + 真实写文件，内容检查 6/6 | 48s |
| `TestLiveSkillCreatorOutputPath` | 技能创建输出路径 | 43s |

### 3. 集成 / 功能验证层（20 个包、200+ 单元测试）

- MCP 客户端 stdio 链路（`internal/mcp/mcp_local_test.go`，本地 node server 实测 Connect → ListTools → CallTool）
- 压缩 token 估算 / 阈值、teams 并发、记忆提取、权限分层、hooks 触发等

### 小结

**项目现状是"功能正确性验证"为主，"agent 能力测评"只有 1 个 token 量化指标 + 3 个冒烟 live 测试**——没有任务级 / 能力级测评（如"完成 N 个真实任务的成功率"）。

## 二、业内主流测评手段

| 类别 | Benchmark | 测什么 | 关键指标 |
| --- | --- | --- | --- |
| **软件工程任务**（黄金标准） | **SWE-bench Verified**（500 手工验证实例）/ **Pro**（731 多语言）/ CodeClash（新） | 真实 GitHub issue → agent 产出 patch → 跑测试套件 | % Resolved（执行式判分，无 LLM 裁判） |
| **终端任务** | **Terminal-Bench 2.0**（89 个任务） | Docker 沙箱内完成真实终端任务（搭系统、写引擎），状态断言 | 成功率（Harbor harness，Claude Code/Codex 通用） |
| **工具调用 + 策略遵循** | **τ-bench**（Sierra） | 模拟用户 + 策略文档 + API 工具，多轮对话后比对数据库终态 | **pass^k**（k 次全成功的概率，比 pass@1 苛刻得多） |
| **通用长链推理** | **GAIA**（466 题、3 难度） | 跨工具链式推理（搜索+计算+多模态）到唯一答案 | 准确率（人类 92% vs 首发模型 15%，差距即测量） |
| **计算机使用** | **OSWorld**（369 VM 任务）、WebArena / VisualWebArena | 真实桌面 / 网页操作，执行式判分 | 成功率 |
| **代码生成** | HumanEval、LiveCodeBench、Aider Polyglot、SWE-Lancer | 单函数生成 / 编辑 / 真实计费任务 | pass@k |
| **科学 / ML** | MLE-bench（Kaggle 任务）、MLGym-Bench、ReplicationBench | 端到端科研复现 | 奖牌率等 |
| **评估框架** | OpenAI Evals、UK AISI Inspect、LangSmith、Harbor、OpenHands harness | 自己搭评测流水线跑上述 benchmark | — |

### 2026 年的两个重要趋势

1. **可信度危机**：Berkeley RDI 审计发现 SWE-bench / Terminal-Bench / OSWorld / GAIA 等全部 8 个主流 benchmark **可被 exploit**（不改任何任务，用 trojan / 答案泄漏 / 环境操纵拿 100%）——"能跑通 benchmark"和"真实能力强"的鸿沟被量化。业界转向防泄漏设计、隐藏答案、隔离判分。
2. **可靠性指标**：τ-bench 的 **pass^k** 取代 pass@1 成为主流声音——生产环境的问题不是"能不能"，而是"能不能每次都行"。OpenAI / Anthropic 的官方 agent 发布都附带 pass^k 类稳定性数据。

## 三、对本项目的建议（按性价比）

1. **自建 mini 任务测评**（最推荐，可演示）：借鉴 Terminal-Bench 思路——用现有 OS 沙箱（bwrap / seatbelt）+ 一组真实任务（如"给项目加一个命令并跑通测试"）+ 状态断言，输出成功率。这是面试可讲的"自建 eval harness"。
2. **可靠性测试**：把现有 live 测试跑 N 次统计通过率（τ-bench pass^k 的轻量版），能暴露 agent 的不稳定问题。
3. **预算允许**：接 SWE-bench 需要 Docker + 数百次 LLM 调用，成本高，实习项目不建议。

## 四、自建 mini 任务测评（已实现）

建议 1 已落地为 `internal/eval/` 包——Terminal-Bench 风格的轻量 eval harness。

### 设计

- **`internal/eval/task.go`**：6 个离线任务（node / git，零网络依赖），每个任务 = 指令 + 环境准备（Setup）+ 状态断言（Verify，执行式判分，无 LLM 裁判）
- **`internal/eval/harness.go`**：Harness 隔离运行（每个任务独立子目录 + 超时控制）+ 成功率报告；Runner 抽象让 harness 与 LLM 解耦（单测用 stub，live 用真实 agent）
- **`internal/eval/harness_test.go`**：harness 逻辑单测（隔离性、断言、失败传播、超时切断、报告数学）
- **`internal/eval/eval_live_test.go`**：真实 LLM 驱动（`MYGOCODE_LIVE_TESTS=1` 门控，`evalConfig` 自动找 `.mygocode/config.yaml`）

### 任务集

20 条任务、5 大能力分类（依据 `精简版Terminal-Bench (Terminal Agent Benchmark).md` 设计，每条任务介绍见该文档第五节）：

| 分类 | 任务 | 测什么 |
| --- | --- | --- |
| **Setup & Build**（环境搭建与依赖编译） | `hello-node` | 文件创建 + 内容正确性 |
| | `go-build-fix` | 缺失 Go 包补全 + 整体可编译可运行 |
| | `node-project-fix` | 按调用方约定补全被依赖模块 |
| | `docker-build-fix` | Docker 构建失败修复（缺失 COPY 文件） |
| **Debugging & Diagnosis**（深入调试与 Bug 修复） | `fix-failing-test` | 读懂代码修 bug（SWE-bench 风格） |
| | `fix-syntax-error` | 语法错误定位与修复 |
| | `log-crash-diagnosis` | 日志驱动调试：读堆栈 → 定位代码 → 修复 |
| | `edit-precision` | 精确编辑：结构相似代码只改目标处，不误伤邻近 |
| | `refactor-export` | 跨文件重命名 + 引用一致性 |
| **Git & VCS Ops**（Git 高级版本控制） | `git-commit` | git 基础提交（含提交信息要求） |
| | `git-merge-conflict` | 真实冲突解决（两分支改同一行） |
| | `git-revert-bug` | 历史定位引入 bug 的 commit 并 revert |
| **CLI Data Processing**（系统配置与文件处理） | `config-update` | JSON 精确改写 + 保持合法 |
| | `search-and-fix` | 多文件搜索定位 + 精准修改 |
| | `batch-string-replace` | 批量替换防误伤（保护相似标识符） |
| **Deploy & Automation**（跨工具与部署自动化） | `cli-countdown` | CLI 工具实现（参数 + 输出格式） |
| | `write-http-server` | HTTP 服务实现（harness 启动 + 真实请求验证） |
| | `docker-run-node` | 容器化应用（写 Dockerfile + harness 真实 build/run） |
| | `docker-compose-up` | Compose 编排（真实 up + HTTP 验证 + down 清理） |
| | `write-tests` | 为已有代码编写测试 |

### 用法

```bash
# 全套（20 任务，约 9-10 分钟，真实 LLM；Docker 任务需 daemon 运行）
MYGOCODE_LIVE_TESTS=1 go test ./internal/eval -run TestEvalLiveMiniSuite -v -count=1

# 单任务迭代（最后一个参数是任务名）
MYGOCODE_LIVE_TESTS=1 go test ./internal/eval -run TestEvalLiveSingleTask -v -count=1 -args hello-node

# 仅 harness 单测（不需要 API key）
go test ./internal/eval
```

### 实测结果

**6 任务版（2026-08-05，claude-sonnet-4-6 经代理）**

首次运行 **3/6（50%）**，失败三个：`refactor-export`（改名未改逻辑）、`search-and-fix`（改错文件）、`cli-countdown`（文件未生成）。排查发现两个**真实环境/产品问题**并修复后，重跑 **6/6（100%）**，总耗时 1m22s：

| 问题 | 根因 | 修复 |
| --- | --- | --- |
| agent 文件操作落到错误目录 | ReadFile/WriteFile/EditFile/Glob/Grep 的相对路径基于**进程 cwd** 解析（go test 进程 cwd 是包目录；真实 TUI 里进程 cwd 恰为项目根所以未暴露），eval 期间 agent 相对路径写入会污染仓库 | 5 个工具增加 `WorkDir` 字段 + `resolveToolPath`（`CreateDefaultToolsWithWorkDir` 统一注入；remote 模式同样受益） |
| agent Bash 工具不可用 | 本机 `bash` 是 WSL bash（PATH 为空、文件系统隔离），`node: command not found` | 任务改为 outcome-driven（验证由 harness 独立执行，不依赖 agent 运行命令） |

附带收益：修复 WorkDir 后任务耗时大幅下降（如 `refactor-export` 103s → 13.7s），因为 agent 不再在错误目录中反复试错。

**10 任务版（2026-08-05，claude-sonnet-4-6 经代理）**

新增 4 个任务（`edit-precision`、`write-tests`、`fix-syntax-error`、`config-update`），首轮 **9/10（90%）**——唯一失败的 `edit-precision` 是 Verify 断言自身缺陷：`"return 20"` 包含子串 `"return 2"`，`Contains` 检查自相矛盾（agent 实际已改对，trace 可证）。修正断言为分号精确匹配后，重跑 **10/10（100%）**，总耗时 2m24s。

| 任务 | 耗时 | 说明 |
| --- | --- | --- |
| hello-node | 7.8s | |
| fix-failing-test | 13.6s | |
| refactor-export | 15.7s | |
| search-and-fix | 16.1s | |
| git-commit | 33.7s | |
| cli-countdown | 7.8s | |
| edit-precision | 10.6s | 精确编辑：只改 second，first 未被误伤 |
| write-tests | 15.1s | 测试断言全对，输出 PASS |
| fix-syntax-error | 12.6s | 定位 `functoin` 拼写错误 |
| config-update | 11.3s | JSON 保持合法，其他字段未动 |

**17 任务版（2026-08-05，claude-sonnet-4-6 经代理）**

按 `精简版Terminal-Bench` 设计重排为 5 大分类，新增 7 个任务（`go-build-fix`、`node-project-fix`、`log-crash-diagnosis`、`git-merge-conflict`、`git-revert-bug`、`batch-string-replace`、`write-http-server`）。首轮 15/17——2 个失败均为**任务设计问题**（非 Agent 能力）：git 任务 Setup 在本机默认分支为 master 时 `checkout main` 失败；`log-crash-diagnosis` 的 Prompt 未指定期望输出导致两种合理方案歧义。修复后重跑 **17/17（100%）**，总耗时 7m32s：

| 任务 | 耗时 | 说明 |
| --- | --- | --- |
| hello-node | 9.5s | |
| go-build-fix | 29.5s | 补全缺失 Go 包，编译 + 运行通过 |
| node-project-fix | 39.1s | 按 main.js 调用约定补全 lib/helper.js |
| fix-failing-test | 11.4s | |
| fix-syntax-error | 12.5s | |
| log-crash-diagnosis | 16.9s | 读堆栈 → 修 null 崩溃 → 输出 a,b,d |
| edit-precision | 11.7s | |
| refactor-export | 24.2s | |
| git-commit | 30.8s | |
| git-merge-conflict | 77.8s | 真实 merge 冲突解决（最耗时） |
| git-revert-bug | 45.1s | git 历史定位 + revert |
| config-update | 13.0s | |
| search-and-fix | 49.0s | |
| batch-string-replace | 44.8s | OLD_TOKEN 全换，OLD_TOKEN_SAFE 未动 |
| cli-countdown | 9.9s | |
| write-http-server | 9.8s | harness 启动服务 + 真实 HTTP 请求验证 |
| write-tests | 16.7s | |

**20 任务版（2026-08-05，claude-sonnet-4-6 经代理，Docker Desktop 已启动）**

按 Terminal-Bench 设计补回容器类别，新增 3 个 Docker 任务（`docker-build-fix`、`docker-run-node`、`docker-compose-up`）。全套 **20/20（100%）**，总耗时 9m08s：

| 任务 | 耗时 | 说明 |
| --- | --- | --- |
| hello-node | 8.4s | |
| go-build-fix | 17.9s | |
| node-project-fix | 17.4s | |
| docker-build-fix | 36.5s | 修复缺失 COPY，真实 docker build + run |
| fix-failing-test | 19.8s | |
| fix-syntax-error | 120.9s | 本次模型响应较慢，仍通过 |
| log-crash-diagnosis | 20.9s | |
| edit-precision | 16.7s | |
| refactor-export | 16.8s | |
| git-commit | 20.0s | |
| git-merge-conflict | 64.1s | |
| git-revert-bug | 42.9s | |
| config-update | 13.7s | |
| search-and-fix | 17.4s | |
| batch-string-replace | 38.1s | |
| cli-countdown | 8.6s | |
| write-http-server | 18.8s | |
| docker-run-node | 15.4s | Agent 写的 Dockerfile 一次构建成功 |
| docker-compose-up | 17.6s | 编排 + 端口映射经真实 HTTP 验证，down -v 清理 |
| write-tests | 16.7s | |

### 与业内手段的对应

| 本项目 | 业内对应 |
| --- | --- |
| Verify 状态断言、无 LLM 裁判 | SWE-bench / Terminal-Bench 的执行式判分 |
| 任务独立隔离目录 + 超时 | Terminal-Bench 的 Harbor 沙箱任务格式 |
| outcome-driven（不管过程只看结果） | Terminal-Bench 2.0 任务公式 |
| 成功率报告 + 单任务迭代 | 主流 leaderboard 的 % Resolved |

> 注：本机 `bash` 曾解析为 WSL bash（PATH 为空），导致 Bash 工具不可用——已修复：`internal/tools/bash.go` 的 `resolveBashPath()` 在 Windows 上优先 Git for Windows 的 bash（继承 Windows PATH），显式拒绝 WSL 启动器（`TestResolveBashPath`/`TestBashToolRunsNode` 守护）。详见 `精简版Terminal-Bench` 文档第五节。
